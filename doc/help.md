## 数据结构

数据结构是以某种方式组合起来的数据元素集合,以及作用在这些元素上的操作。

常用的数据结构有序列、映射和集合。

### 序列

通过位置索引访问序列中的元素，负索引表示序列末尾元素的位置
常用的序列有字符串、列表、元组, 其中列表支持修改而元组不能修改。
内置函数返回的都是元组，几乎所有情况都可以使用列表代替元组，但将元组用作字典键时不能用列表代替，因为字典的键不允许修改。

通用序列操作

- 索引 `[index]`
- 切片 `[start, end, step]`
- 扩展 (`+`、`*`)
- 迭代 （for in）
- 成员检查 (in 、not in)
